# Signaling server URL (required for the app to connect peers)
VITE_SIGNALING_URL=wss://your-signaling-service.onrender.com

# Render deploy hook URL (required for `npm run deploy` option 2)
# Get this from: Render dashboard → Service → Settings → Deploy Hook
RENDER_DEPLOY_HOOK_URL=https://api.render.com/deploy/srv-xxxxx?key=yyyyy
