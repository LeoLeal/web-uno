## 1. Utilities

- [x] 1.1 Create `lib/room-code.ts` with:
    - `generateRoomId()` (lowercase)
    - `formatRoomId(id)` (Happy Lions [42])
    - `normalizeRoomId(input)` (happy-lions-42)

## 2. Room Creation

- [x] 2.1 Update `app/create/route.ts` to use `generateRoomId()`.

## 3. Home Page UX

- [x] 3.1 Update `app/page.tsx` input logic to normalize spaces/symbols to hyphens before joining.

## 4. Lobby UI

- [x] 4.1 Update `app/room/[id]/page.tsx` to display the formatted Room ID in the header.

