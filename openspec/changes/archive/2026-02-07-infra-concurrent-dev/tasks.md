## 1. Setup

- [x] 1.1 Install `concurrently` as a dev dependency

## 2. Implementation

- [x] 2.1 Rename existing `dev` script to `dev:next`
- [x] 2.2 Create `dev:signaling` script (alias to `signaling`)
- [x] 2.3 Create new `dev` script using `concurrently` with colored output
- [x] 2.4 Verify `npm run dev` starts both services
