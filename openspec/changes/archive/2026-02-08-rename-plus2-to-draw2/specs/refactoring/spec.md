## No Spec Changes

This is a pure refactoring change with no new or modified capabilities.

The `plus2` → `draw2` rename affects only:

- File naming conventions (assets)
- TypeScript type literals
- Test assertions

No functional behavior changes are introduced.
