## 1. Implementation

- [ ] 1.1 Update `app/globals.css` to add `outline: none` to `dialog.modal`.
- [ ] 1.2 Verify focus states of elements *inside* the modal are not affected (accessibility check).
